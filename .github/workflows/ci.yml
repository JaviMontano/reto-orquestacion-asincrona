name: verify

on:
  push:
  pull_request:

jobs:
  docker-verification:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - name: Run the same verification used locally
        run: ./scripts/verify.sh
